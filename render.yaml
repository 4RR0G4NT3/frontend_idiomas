services:
  - type: web
    name: front-idiomas
    runtime: static
    buildCommand: npm install && npm run build
    publishPath: dist/webapp/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
