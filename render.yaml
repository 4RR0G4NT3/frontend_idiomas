services:
  - type: web
    name: front-idiomas
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/webapp/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
